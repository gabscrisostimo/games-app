# games-app — projeto pessoal

Projeto pessoal do Gabs, **sem nenhuma relação com a Alaska Academy**.

## Isolamento (importante)

- Este projeto vive **fora** de `~/projects` de propósito: namespace de memória próprio, separado do da Alaska.
- **Ignore o contexto da Alaska** nesta sessão (SOT, professoras, produtos, skills `alaska-*`, contratos, infra da escola). Nada disso se aplica aqui.
- Config deste projeto (skills, settings, hooks) mora em `./.claude/` — **nunca** em `~/.claude/`, que é sincronizado com a infra da Alaska.
- Não editar nem rodar git em `~/projects/alaska_docs` ou `~/projects/claude_config` a partir daqui.

## Sobre o projeto

(a preencher — descrever o que é o games-app, stack, objetivos)
